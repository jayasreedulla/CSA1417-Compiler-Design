%{
#include <stdio.h>
%}

%%
if|else|while|int|switch|for|char    { printf("%s -> KEYWORD\n", yytext); }
[A-Za-z_][A-Za-z0-9_]*              { printf("%s -> IDENTIFIER\n", yytext); }
[ \t\n]+                           ;
.                                  ;
%%

int yywrap()
{
    return 1;
}

int main()
{
    printf("Enter input:\n");
    yylex();
    return 0;
}
